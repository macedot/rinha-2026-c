x-api-common: &api-common
  build: .
  volumes:
    - rinha-c-sockets:/sockets
  networks:
    - rinha-c-net
  ulimits:
    nofile:
      soft: 65535
      hard: 65535
  deploy:
    resources:
      limits:
        cpus: "0.4"
        memory: "160M"
  environment: &api-env
    INDEX_PATH: "/app/resources/index.bin"
    LISTEN_TCP: "0"
    UDS_MODE: "666"
    UNLINK_UDS: "1"
    IVF_NPROBE: "8"
    IVF_FULL_NPROBE: "24"
    CANDIDATES: "0"

services:
  nginx:
    image: nginx:alpine
    container_name: rinha-c-nginx
    ports:
      - "9999:9999"
    volumes:
      - rinha-c-sockets:/sockets
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api1
      - api2
    networks:
      - rinha-c-net
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: "50M"

  api1:
    <<: *api-common
    container_name: rinha-c-api1
    hostname: api1
    environment:
      <<: *api-env
      UDS_PATH: "/sockets/api1.sock"

  api2:
    <<: *api-common
    container_name: rinha-c-api2
    hostname: api2
    environment:
      <<: *api-env
      UDS_PATH: "/sockets/api2.sock"

networks:
  rinha-c-net:
    driver: bridge

volumes:
  rinha-c-sockets:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: size=10m
